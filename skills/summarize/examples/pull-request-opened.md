👍🏻 Boss, I [rewrote](https://claude.ai/epitaxy/session_01H8bRtP3KqceUVm9LaW7nrZ)
  the JSON writer in the `@foobar/xyz` repo, replaced
  the hand-rolled encoder in `src/io/serial.rs` with the
  `serde_json` driver on branch `fix/escape-quotes`, and
  opened [pull request #128](https://github.com/foobar/xyz/pull/128)
  for review; the CI run is green on the unit suite and
  I will land the merge once `@joshua` clears the
  `release-blocker` label.
